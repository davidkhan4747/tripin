services:
  nextjs:
    image: node:20-alpine
    container_name: tripin-lading
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3547:3000"
    environment:
      - NODE_ENV=production
    command: >
      sh -c "npm install &&
             npm run build &&
             npm start"
